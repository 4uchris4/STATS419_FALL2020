# libraries

library(pryr);			#mem_used()
library(lmtest);		#lmtest has lrtest
library(MASS);			#polr

library(tractor.base);	#implode

library(stringr);
library(rvest);			#imdb web scraping
library(devtools); 		# this allows for source_url(githublink)

source_url("https://raw.githubusercontent.com/4uchris4/STATS419_FALL2020/master/functions/functions-imdb.R");
